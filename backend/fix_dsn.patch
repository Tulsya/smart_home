--- a/main.go
+++ b/main.go
@@ -115,7 +115,9 @@ func initMetrics() {
 }
 
 func initPostgres(dsn string) {
+	log.Printf("DEBUG: PostgreSQL DSN = %s", dsn)
 	var err error
 	psqlConn, err = sql.Open("postgres", dsn)
 	if err != nil {
 		log.Fatalf("Ошибка подключения к PostgreSQL: %v", err)
